apiVersion: apps/v1
kind: Deployment
metadata:
  name: reeve-micro
  labels:
    app: reeve-micro
  namespace: raman
spec:
  replicas: 1
  selector:
    matchLabels:
      app: reeve-micro
  template:
    metadata:
      labels:
        app: reeve-micro
    spec:
      containers:
        - name: reeve-micro
          image: ramann123/ssecure-raman-customer-orders-service:latest
          ports:
            - containerPort: 5000
          resources:
            limits:
              cpu: "250m"
              memory: "256Mi"


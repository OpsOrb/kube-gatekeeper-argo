apiVersion: v1
kind: Service
metadata:
  name: reeve-micro
  namespace: raman
spec:
  selector:
    app: reeve-micro
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: NodePort

